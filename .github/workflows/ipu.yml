name: caller workflow

on:
  workflow_call:

jobs:
  validation:
    name: Server Validation
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    permissions: 
      id-token: write
      contents: read

    steps:
      - name: checkout
        uses:  actions/checkout@v7

      - name: validation
        uses: Upskill-DevOps/tets-repo/actions/server-validation@main
          
